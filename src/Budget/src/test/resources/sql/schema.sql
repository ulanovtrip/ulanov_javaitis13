create table budget (
                        id int auto_increment primary key,
                        title varchar(20),
                        sum int,
                        date_time varchar(40)
);